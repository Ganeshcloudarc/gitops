import rospy


def topic_status(topic_info_dict, timeout):
    """
    Checks whether data is available on data.
    Params:
        topic_info_dict ( dicts): {{"topic_name1" : "msg type object1"},{"topic_name2" : "msg type object2"}}
        timeout : max time to check data in topics
    Returns:
        status (Bool), list_status
    """
    pass
